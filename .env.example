# Google Translate API Key (Optional)
# The current implementation uses Google Translate Widget (free, client-side)
# This key is for Google Cloud Translation API if you want to upgrade to server-side translation
GOOGLE_TRANSLATE_API_KEY=your_google_translate_api_key_here
NEXT_PUBLIC_GOOGLE_TRANSLATE_API_KEY=your_google_translate_api_key_here

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace 'your_google_translate_api_key_here' with your actual API key
# 3. Never commit the .env file (it's already in .gitignore)
